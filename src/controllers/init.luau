--!strict

-- i just don't want to name them as SomethingController each time
-- also controllers.something looks cool

return {
	container = require("@self/container"),
	tab = require("@self/tab"),
	button = require("@self/button"),
	switch = require("@self/switch"),
}
